eclipse che +  volume mount remote-endpoint belonging to the container component tools + volume mount plugins belonging to the container component tools

schemaVersion: 2.1.0
metadata:
  name: python-hello-world
  namespace: ws-development-py
projects:
  - name: ws-development-py
    zip:
      location: 'http://devfile-registry.openshift-operators.svc:8080/resources/v2/python-hello-world.zip'
components: []
commands:
  - exec:
      commandLine: python3 hello-world.py
      component: tools
      group:
        kind: run
      label: Run application
      workingDir: '${PROJECT_SOURCE}'
    id: run-application
